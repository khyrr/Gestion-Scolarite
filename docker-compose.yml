services:
  # Laravel Application
  app:
    env_file: .env.docker
    build:
      context: .
      dockerfile: Dockerfile
    container_name: gestion-scolarite-app
    restart: unless-stopped
    working_dir: /var/www/html
    volumes:
      - ./:/var/www/html
      - ./docker/php/php.ini:/usr/local/etc/php/conf.d/custom.ini
    ports:
      - "8000:8000"
    environment:
      - APP_NAME=${APP_NAME}
      - APP_ENV=${APP_ENV}
      - APP_KEY=${APP_KEY}
      - APP_DEBUG=${APP_DEBUG}
      - APP_URL=${APP_URL}
      - QUEUE_CONNECTION=database
      - REDIS_HOST=redis
      - REDIS_PASSWORD=null
      - REDIS_PORT=6379
    depends_on:
      - db
      - redis
    networks:
      - laravel

  # Queue Worker
  queue:
    env_file: .env.docker
    build:
      context: .
      dockerfile: Dockerfile
    container_name: gestion-scolarite-queue
    restart: unless-stopped
    working_dir: /var/www/html
    volumes:
      - ./:/var/www/html
      - ./docker/php/php.ini:/usr/local/etc/php/conf.d/custom.ini
    environment:
      - APP_NAME=${APP_NAME}
      - APP_ENV=${APP_ENV}
      - APP_KEY=${APP_KEY}
      - APP_DEBUG=${APP_DEBUG}
      - APP_URL=${APP_URL}
      - QUEUE_CONNECTION=database
      - REDIS_HOST=redis
      - REDIS_PASSWORD=null
      - REDIS_PORT=6379
    depends_on:
      - db
      - redis
      - app
    command: php artisan queue:work --verbose --tries=3 --timeout=90
    networks:
      - laravel

  # Redis for caching and sessions
  redis:
    image: redis:7-alpine
    container_name: gestion-scolarite-redis
    restart: unless-stopped
    ports:
      - "6380:6379"
    networks:
      - laravel

  # MySQL service removed — using PostgreSQL (`pg`) instead.
  # If you need MySQL again, re-add a `db` service using the previous configuration.

  # phpMyAdmin (optional - for database management)
  phpmyadmin:
    env_file: .env.docker
    image: phpmyadmin/phpmyadmin
    container_name: gestion-scolarite-phpmyadmin
    restart: unless-stopped
    environment:
      PMA_HOST: db
      PMA_PORT: 3306
      PMA_USER: ${DB_USERNAME}
      PMA_PASSWORD: ${DB_PASSWORD}
    ports:
      - "8080:80"
    depends_on:
      - db
    networks:
      - laravel

  # MySQL Database
  db:
    env_file: .env.docker
    image: mysql:8.0
    container_name: gestion-scolarite-db
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: ${DB_DATABASE}
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
      MYSQL_USER: ${DB_USERNAME}
      MYSQL_PASSWORD: ${DB_PASSWORD}
      MYSQL_ALLOW_EMPTY_PASSWORD: 'no'
    volumes:
      - dbdata:/var/lib/mysql
      - ./docker/mysql/my.cnf:/etc/mysql/conf.d/my.cnf
    ports:
      - "3307:3306"
    networks:
      - laravel

networks:
  laravel:
    driver: bridge

volumes:
  dbdata:
    driver: local
